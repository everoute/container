{{ range . -}}
{{ .Path }}{{ range $include_file := .Includes }} {{ $include_file }}{{ end }} {
  su root root
  rotate {{ .MaxFile }}
  size {{ .MaxSize }}M
  copytruncate
  compress
  missingok
  notifempty
  # use date as a suffix of the rotated file
  dateext
}

{{ end -}}
